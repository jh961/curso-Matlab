% función que devuelve la suma y la resta de dos número
function [sum, res, s,m,M] = suma_resta(a,b) 

if nargin == 0
    error(' la función necesita argumentos')
end

sum = a+b;
res = a-b;
s = sind(res);
r = pi
end

